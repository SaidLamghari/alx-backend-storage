// File 100-find that Write a script that lists all documents with name starting by Holberton in the collection school
db = db.getSiblingDB('<DATABASE_NAME>');
db.school.find({ name: { $regex: '^Holberton' } }).forEach(printjson);
