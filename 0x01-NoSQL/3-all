// File : 3-all taht List all documents in the collection school

// Switch to the specified database (my_db)
use my_db

// Query and print all documents in the school collection
db.school.find().forEach(printjson)
